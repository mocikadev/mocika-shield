# 应用使用情况分享回归记录

> 2026-09-18：以下保留功能开发阶段的测试证据；末尾另记维护者授权后的 Beta.6 发布复验。不要把本地旧版本构建和新的正式发布产物混为一谈。

## 验证范围

桌面 GUI 的身份识别、按包名偏好、成功任务分享、单行控件及独立 Worker 协议。不修改核心加固、签名算法或 Android 壳，不把本次测试当作 Android 运行兼容矩阵复测。

## 自动化与评审

| 验证 | 实际结果 |
|---|---|
| GUI 后端最终全量 | 105 项通过、2 项环境用例默认忽略；主代理复跑确认 |
| 新增真实 APK 身份与本地 HTTP 环境用例 | 显式设置两份自建 APK 路径后单独执行，1 项通过；不是未经执行的忽略项 |
| GUI 前端 | 11 项行为测试通过（分享 7 项），构建及 lint 通过 |
| 共享核心与 CLI | 153 项通过、5 项既有环境用例忽略 |
| Worker | 38 项通过；本地真实 SQLite，非远端 D1 部署验收 |
| 维护脚本 | 21 项通过 |
| 格式与差异 | `cargo fmt --all -- --check`、`git diff --check` 通过 |
| Clippy | 未新增告警；仍有证书存储、旧 Manifest 解码、任务完成测试位置、匿名统计测试初始化的 4 个既有告警，不宣称零告警 |

身份/偏好、服务端、客户端三个阶段均经过独立评审及修复复审。整阶段最终评审提出的严格 Manifest 外层类型、头长、声明长度和完整消费校验已定点修复，新增反例及真实 APK 回归通过，最终增量复审通过，代码阻断项清零；原宽松预检不变。客户端针对检查引用实际过期清理、容量淘汰后无法取消的问题，已补充清理前撤销代次、清队列及会话关闭的回归。

覆盖同包名持久取消、跨页同步、新包默认、快速切包旧响应丢弃、任务冻结、保存失败关闭、输入替换、终态单次消费、取消/失败不发送、自动签名不双计、成功日期冻结、队列上限/过期、固定编号重试、重定向拒绝与 HTTP 超时。上述任务边界为真实状态行为测试，不是桌面点击验收。

## 自建 APK 与真实工具链

仅使用仓库自建样本，临时证书及独立输出目录；没有上传用户业务 APK 信息，没有改变用户证书、应用配置或全局 Java 环境。

- 普通冒烟包：`dev.mocika.shield.smoke`，版本码 `1`，资源名称 `Mocika Shield Smoke`，最低 API 19，无 Native 库。
- 工控 Native 探针：`dev.mocika.shield.api19probe`，版本码 `1`，直写名称 `Mocika API 19 Native 探针`，最低 API 19，`armeabi-v7a`。

探针的 `aapt dump badging` 显示空 label，不能据此推断未设置名称；实际 APK 的 `aapt dump xmltree ... AndroidManifest.xml` 已由主代理与验收代理独立确认上述直写名称，生产解析结果一致。

生产身份入口读取两份真实 APK，经生产协议与 HTTP 发送函数提交到 localhost 接收器，核对只有十个协议字段、工具版本来自编译版本、不含路径或敏感字段。没有连接线上接收接口。该用例不等同于实际 Tauri 任务完成回调端到端测试。

验收代理执行真实 CLI/core 流程：先签名普通冒烟包，分别使用标准资源和 Android 4.4 兼容资源加固，再独立签名；两份输出的 `apksigner verify --verbose` 均确认 v1/v2/v3 有效，`shield check-apk` 均确认已加固、已签名，解出的 `classes.dex` 包含 MSHD 标记。

Native 探针用作加固输入时触发现有规范库名冲突检查，被安全拒绝；没有将该次尝试记为加固通过，也未为本功能改变冲突规则。兼容资源实际加固验证采用无 Native 的普通冒烟包，不能据此声称所有工控包均通过。

## macOS 应用包

主代理执行 `cargo tauri build --bundles app` 成功，交付：

`target/release/bundle/macos/MocikaShield.app`

- 完整 `.app`，不是裸二进制；arm64，约 24 MiB，本地开发版本 `1.4.0-beta.5`。
- 两份运行时资源及 `apktool.jar`、`apksigner.jar` 均存在，与 Tauri 配置映射的源文件 SHA-256 一致。
- 两份资源 ZIP 的完整性校验通过。
- 重新执行 ad hoc 本地签名，`codesign --verify --deep --strict --verbose=2` 通过；不是 Apple 开发者签名或公证。
- 最终严格 Manifest 外层结构修复后已再次完整构建（58.50 秒，退出 0），重新签名并严格验签；四项资源逐字节比对及两份 ZIP 完整性检查均通过。交付以这次最终构建为准。

## 尚未验收

- [ ] 默认 960×780、最小 760×600、宽窗口，中英文、浅深色及键盘/读屏的实际界面检查。
- [ ] 通过真实 Tauri 界面操作验证跨页记忆、自动签名完整流程及结果快照；目前有行为测试和代码接入检查，不能替代桌面操作。
- [ ] Windows/Linux 原生运行。
- [x] 维护者追加授权后，远端增量迁移、关闭状态旧通道验证、新通道启用与线上自建样本验收已完成，见[上线记录](../ops/application-usage-sharing.md)。

桌面工具两次返回 `Native pipe startup failed`，没有取得新界面截图，不以静态代码或构建成功替代视觉验收。已发布 Beta.5 不产生这类分享数据。

## Beta.6 发布复验

2026-09-18 已同步 `1.4.0-beta.6` 清单、前端版本、根锁文件和 API19 独立锁文件，没有更新第三方依赖。重新执行 GUI 后端 105 项通过（2 项默认忽略）、真实双 APK 身份与本地 HTTP 环境用例显式 1 项通过、前端 11 项通过、前端构建及 lint 通过、Worker 38 项通过、核心与 CLI 153 项通过（5 项既有忽略）、维护脚本 21 项通过、Rust 格式检查通过。

重新构建 Beta.6 完整 macOS `.app`（54.74 秒，退出 0），四项内置资源逐字节比对、两份 ZIP 完整性及 ad hoc 严格验签均通过。本地包仍仅 arm64、未公证；正式通用包由发布工作流生成，不将本地包冒称三平台产物。

远端自建样本已确认版本关联、两种操作、去重、错误响应、私有读取隔离与分钟限流；专用测试数据清理完成，详见运维记录。实际 Tauri 界面交互、自动签名人工闭环、Windows/Linux 原生运行仍未验收。第一次发布就绪检查仅因预期的未提交工作区未通过，合并及打标签前必须在干净状态复跑。
