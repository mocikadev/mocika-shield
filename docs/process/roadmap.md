# Mocika Shield 功能路线图

> 本文只回答“现在维护什么、下一步做什么、后面可能做什么”。已完成版本和研究过程见[路线图历史档案](roadmap-history.md)，技术细节见对应设计文档。
> 最后更新：2026-09-01

## 当前基线

| 项目 | 当前结论 |
|------|----------|
| 最新稳定版 | `1.3.0` |
| 最新公开候选版 | `1.4.0-beta.1`；冻结功能与协议，只收集真实兼容性反馈 |
| 正式交付形态 | Linux、macOS、Windows 桌面 GUI；CLI 仅供本地开发、调试和回归，不由 CI 单独生成或发布 |
| Android 支持 | 标准模式支持 Android 5.0 及以上；Android 4.4 工控兼容模式限定 `armeabi-v7a` 范围 |
| 当前维护策略 | `1.3.x` 只处理可稳定复现且影响安装、启动、数据安全或大范围兼容的缺陷；`1.4.0` 进入兼容性收口，不再追加功能 |
| 当前公开问题 | Issue #2 继续观察 16 KB 场景反馈，不把等待反馈作为后续开发前置条件 |

## 当前版本：`1.4.0` 可靠性与使用效率

### Alpha 已完成能力

`1.4.0-alpha.1` 已完成“选择 APK—评估风险—确认方案—执行—查看结果”的单 APK 闭环：加固页按高频操作与低频设置重组，常用方案可持久化，输出文件名和目录可独立调整；同时提供只读风险预检与可复制的脱敏任务诊断。

本版本不改变 DEXB v5、运行时加载路径、缓存协议、Root 默认策略和 Android 最低版本承诺。

### Alpha 收口完成：`1.4.0-alpha.2` 统计可诊断性与 Manifest 调查

`1.4.0-alpha.2` 已补齐真实使用暴露出的工具链兼容性和匿名桌面工具统计诊断，不改变加固协议、默认保护策略或运行时行为。

| 工作项 | 验收标准 |
|---|---|
| 新 Manifest 调查 | 自建含 `enableOnBackInvokedCallback`、`targetSdk 30/35` 的样本已通过现有 apktool 解包、加固、重打包与 Manifest 保留回归；Issue #109 尚未获得真实样本，保持等待样本状态，不将未复现问题宣称为已修复，也不据此升级工具链 |
| 资源格式兼容 | 解包后识别 `res/` 中内容为 JPEG 但扩展名为 `.png` 的普通资源，无损改为同名 `.jpg` 后再重打包；九宫格资源或同名目标已存在时明确失败，绝不覆盖或转码 |
| 统计版本维度 | 保留已有随机匿名实例标识与用户开关；按日期、工具版本聚合桌面端启动、加固和签名计数，不增加机器或最终用户识别 |
| 失败阶段 | 仅上传固定的任务阶段聚合计数，不上传原始日志、路径、APK、包名、证书或密码；上传失败不得影响加固和签名 |
| 数据兼容 | 历史日期聚合继续可用；细分维度缺失时明确标记不可用，不将历史数据伪造为零或按版本重算 |

详细边界与迁移方案见[1.4.0 兼容性与匿名统计可诊断性设计](../superpowers/specs/2026-08-25-1.4-compatibility-observability-design.md)。

### 当前阶段：`1.4.0-beta.1` 兼容性收口

Beta 不再增加功能、格式或默认策略，只围绕现有能力校准兼容性和提示，并形成可以直接转入 RC 的发布证据。

| 工作项 | 验收标准 |
|--------|----------|
| 真实 APK 回归 | 使用已获授权的 Issue、QQ群与本地样本，覆盖标准模式、Android 4.4 工控模式、签名、覆盖安装与冷启动；新增样本先脱敏登记测试结论，不提交 APK、证书或业务资料 |
| 预检校准 | 每项提示均有对应输入依据和可执行建议；已支持场景不得被误报为“必须阻止”，无法可靠判断时必须降级为“有风险” |
| 三平台一致性 | Linux、macOS、Windows 的默认方案、路径规则、输出命名、风险等级和诊断摘要语义一致 |
| 发布审计 | 全量 CI、三平台发布包、正式包内容、版本号、文档、Release Notes 与统计隐私说明全部通过；无阻塞缺陷后发布 `1.4.0-rc.1` |

若 Beta 阶段没有发现阻塞问题，RC 仅复用该候选产物并完成最终审计；稳定版 `1.4.0` 不临时追加新能力。

### 明确非目标

- 不增加在线 APK 上传、云端分析或远程证书处理。
- 不把完整 APK 路径、密码、用户目录、业务包名或证书材料写入匿名统计。
- 批量加固暂缓，不作为 `1.4.0` 的范围或后续阶段的前置条件；未来如恢复评估，必须复用单 APK 的预检与任务快照。
- 不把 CLI 作为正式 Release 产物，也不增加单独的 CLI CI 打包任务。
- 不借预检改造 APK 重写、签名、运行时加载或加密协议。

## 后续候选

候选方向不等于已排期版本；只有需求、依赖条件和验收资源同时具备后才进入“下一版本”。

| 方向 | 当前状态 | 进入条件 |
|------|----------|----------|
| 发布供应链完善 | 可先做基础项 | 优先补充产物来源证明、依赖清单和发布可追溯记录；安装包代码签名与 macOS 公证仍需要账号、证书及持续维护投入 |
| AAB 正式支持 | 条件受阻 | 已完成单 base 模块可行性实验；需要 Google Play 账号、Play 应用签名和内部测试轨道完成真实闭环，详见 [AAB 可行性结论](../design/aab-feasibility.md) |
| 工具链局部轻量化 | 待专项原型 | 先用数据证明 apktool 或整包重建是主要瓶颈；未知结构必须保留可靠回退，详见 [工具链研究](../design/toolchain-modernization.md) |
| 可选资源保护 | 需求驱动 | 有真实用户场景、资源选择和应用侧读取接口设计；不通过 Hook 透明拦截，详见 [资源保护研究](../design/asset-protection.md) |
| 供应链成熟度 | 持续评估 | 代码签名、公证和完整证明需要相应账号、证书与维护成本 |

## 已暂停或停止的方向

| 方向 | 状态 | 结论 |
|------|------|------|
| API 29 以上完整内存 DEX | 暂停产品接入 | 公开加载链路和回退状态已完成大量实验，但真实大 APK 的长期内存性价比门禁未通过；不再占用 `1.4.0` 版本号，详见 [内存 DEX 设计](../design/memory-dex-production.md) |
| DEX 结构与方法代码分离 | 停止产品接入 | 离线协议、真实多 DEX 和攻击测试已完成；公开运行时仍要求形成完整 DEX，继续只能依赖高风险私有运行时或独立虚拟机，详见 [代码分离研究](../design/dex-code-separation.md) |
| ART/系统函数 Hook、自修改代码、可写可执行内存 | 禁止 | 与多版本兼容、安全软件风险和项目安全边界冲突，不进入正式方案 |
| 独立在线加固服务 | 不计划 | 项目保持本地离线处理，不托管用户 APK、证书或密码 |

## 并行维护规则

- `main` 始终保持可发布；复杂功能使用临时 `feat/*` 或 `fix/*` 分支，合并后删除。
- 稳定版 Issue 和 QQ 群反馈持续观察，但只有可复现高优先级缺陷才抢占当前开发任务。
- 一个小版本聚焦一个主题；预检、运行时安全、AAB 和工具链重写不得混成一次发布。
- Alpha 可以完成计划内能力；Beta 起冻结功能与协议；RC 只修阻塞正式发布的问题。
- 每个阶段形成完整代码、测试和文档后再提交，不因细小修改堆积多个 PR。
- 已完成内容不再复制回本文件；版本结果进入 Release Notes 或[路线图历史档案](roadmap-history.md)，实验数据进入对应设计文档。

## 下一步执行顺序

1. 发布 `1.4.0-beta.1`，继续收集真实 APK 的兼容性反馈；只修复阻塞加固、签名、安装或启动的问题，不再追加功能或统计字段。
2. 对 Issue #109 仅在收到经授权的真实样本后复现和定位；结论写入兼容性记录，不提交用户 APK。
3. Beta 无阻塞问题后冻结为 RC，完成发布审计并发布稳定版 `1.4.0`。
