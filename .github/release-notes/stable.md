# Mocika Shield {{tag}}

这是当前的 **{{release_kind}}**。本版本的具体变更见下方自动生成列表。

This is the current **stable release**. See the generated changelog below for version-specific changes.

## 下载 / Downloads

- Windows：`MocikaShield_{{version}}_windows_x64_setup.exe`
- macOS：`MocikaShield_{{version}}_macos_universal.dmg`
- Linux：选择 `.AppImage` 或 `.deb` 安装包 / Choose the `.AppImage` or `.deb` package

## 使用须知 / Notes

- 需要完整 JDK 8 或更高版本（含 `java` 与 `keytool`） / A full JDK 8 or later with `java` and `keytool` is required
- 加固、签名和证书资料均在本地处理，不会上传用户文件 / APKs, signing keys, and certificates are processed locally and never uploaded
- 安装包尚未进行商业代码签名或公证；首次运行请参考 README / Installers are not currently code-signed or notarized; see the README before the first launch
- 当前正式支持 APK；CLI 可从源码构建，但 Release 仅提供桌面 GUI / APK is currently supported; the CLI can be built from source, while Releases provide desktop GUI packages only

## 本次变更 / What's Changed

以下列表由 GitHub 根据本版本与上一版本之间的拉取请求和提交自动生成。

The following changelog is generated by GitHub from pull requests and commits since the previous release.
