# Mocika Shield {{tag}}

这是一个 **{{release_kind}}**，用于提前验证当前版本的构建产物、GUI 交互和签名/加固流程。

## 本次说明

- 版本号：`{{version}}`
- 发布类型：{{release_kind}}
- 适用场景：内测、自测、流程回归、正式版前验收

## 使用前请注意

- 仍建议优先使用原始、已签名的 APK 进行加固
- 如果你已经在证书页保存默认证书，签名页与加固页自动签名会共用同一套证书资料
- 这是候选版本，界面细节和说明文本在正式版前仍可能继续调整

## 自动生成的变更列表

以下内容由 GitHub 根据本次版本与上一个版本之间的提交自动生成。
