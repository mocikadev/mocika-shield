# Mocika Shield {{tag}}

这是一个 **{{release_kind}}**，仅用于提前验证构建产物和核心流程，不建议用于生产环境。

This is a **pre-release** for validating build artifacts and core workflows. It is not recommended for production use.

## 下载 / Downloads

- Windows：`MocikaShield_{{version}}_windows_x64_setup.exe`
- macOS：`MocikaShield_{{version}}_macos_universal.dmg`
- Linux：选择 `.AppImage` 或 `.deb` 安装包 / Choose the `.AppImage` or `.deb` package

## 使用须知 / Notes

- 需要完整 JDK 8 或更高版本（含 `java` 与 `keytool`） / A full JDK 8 or later with `java` and `keytool` is required
- 加固、签名和证书资料均在本地处理，不会上传用户文件 / APKs, signing keys, and certificates are processed locally and never uploaded
- 安装包尚未进行商业代码签名或公证 / Installers are not currently code-signed or notarized
- 当前正式支持 APK；Release 仅提供桌面 GUI / APK is currently supported; Releases provide desktop GUI packages only

## 本次变更 / What's Changed

以下列表由 GitHub 根据本版本与上一正式版本之间的拉取请求和提交自动生成，完整覆盖当前版本周期的 Alpha、Beta 与 RC 变更。

The following changelog is generated by GitHub from pull requests and commits since the previous stable release, covering the complete Alpha, Beta, and RC cycle.
